# TODO - Frontend + PgAdmin CRUD for Placement Management

- [ ] Confirm backend is reachable and endpoints match frontend routes.
- [ ] Fix any missing static/frontend wiring (ensure Spring serves `static/index.html`).
- [ ] Ensure backend PostgreSQL connection uses correct password `5454`.
- [ ] Run the backend and load frontend to verify all CRUD operations.
- [ ] Provide a single link (local URL) to access the frontend.

